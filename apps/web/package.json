{
  "name": "@nebenkosten/web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit && vite build",
    "build:html-preview": "node scripts/build-html-preview.mjs",
    "dev": "vite",
    "preview": "vite preview",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@nebenkosten/persistence": "workspace:*",
    "@nebenkosten/schema": "workspace:*",
    "react": "19.2.7",
    "react-dom": "19.2.7"
  }
}
