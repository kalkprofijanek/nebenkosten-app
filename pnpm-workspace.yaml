packages:
  - apps/*
  - packages/*

sharedWorkspaceLockfile: true

# Security floors: GHSA-mh99-v99m-4gvg and GHSA-r28c-9q8g-f849.
# Remove once every upstream dependency requires a patched version itself.
overrides:
  brace-expansion: '>=5.0.8'
  postcss: '>=8.5.18'
